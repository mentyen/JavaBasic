package com.class22_Constructors;

public class ConstructorvsMethod {
	/*
	 * 1.Constructor must have a same name as a class-Method can have any names
	 * 2.Constructor can not be STATIC-Method can be STATIC or not
	 * 3.Private constructor can only be access from a same class
	 * 4.Constructor CANNOT have a RETURN type even void - Method MUSTHAVE a RETURN type or VOID
	 * 5.Constructor will get called automatically - Method needs to be called;
	 * 6.Were is always a default constructor - Method you have to created your self;
	 * 
	 * SIMILARITY:
	 * 1.Both HAVE/MIGHT NOT HAVE parameters;
	 * 2.Access modifiers can be used;
	 * 
	 *
	 * 
	 * 
	 */

}
