package com.class20_AccessModifiers;

public class PublicModifier {
	
	// public,protected(classes in a same package)default(to all classes in a package),private(in a same class)
	//NON access modifiers: static, final,abstract
	//method:access modifier+returnType+name+(parameters);
	//Instance method same as instance variable each object will get his copy

}
